[package]
name = "rust-guard"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "lib"]

[profile.release]
opt-level = "z"     # Optimize for size (UEFI constraint)
lto = true
codegen-units = 1
panic = "abort"     # No unwinding in UEFI

[dependencies]
