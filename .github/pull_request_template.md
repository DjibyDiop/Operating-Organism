## Checklist (required)

- [ ] `oo-guard` prebuild passes on `llama2_efi_final.c`
- [ ] Firmware builds (`make repl`)
- [ ] If touching policy surfaces: verified `/oo_*` behavior with `policy.dplus` and/or `OOPOLICY.BIN`

## Notes

Describe what changed and why.
