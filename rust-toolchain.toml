[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"

# Use the latest stable toolchain to remain compatible with Cargo lockfile version
# generated by contributors. Change to a specific numeric channel once the team
# agrees on an exact pinned version.
