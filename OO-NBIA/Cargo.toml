[package]
name = "oo-nbia"
version = "0.1.0"
edition = "2021"

[lib]
name = "nbia"
crate-type = ["staticlib", "rlib"]

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
